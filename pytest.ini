[pytest]
pythonpath = . mcp-servers/alarm-management
asyncio_mode = auto
testpaths = tests rag/tests
markers =
    integration: spins up a real (in-process or subprocess) service
    e2e: full combined MCP + RAG end-to-end scenario
